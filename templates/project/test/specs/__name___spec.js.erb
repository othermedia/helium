JS.ENV.<%= camelize(@name) %>Spec = JS.Test.describe("<%= camelize(@name) %>", function() { with(this) {
    it("does not have any tests", function() { with(this) {
        assert( false )
    }})
}})

