# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'tom_deployer' do |p|
  # self.rubyforge_name = 'tom_deployerx' # if different than 'tom_deployer'
  p.developer('James Coglan', 'james.coglan@othermedia.com')
  p.extra_deps = %w[grit jake packr sinatra]
end

# vim: syntax=ruby
